import * as React from "react";
interface EmailHeaderProps {
    title: string;
    subtitle?: string;
}
export declare function EmailHeader({ title, subtitle }: EmailHeaderProps): React.JSX.Element;
export {};
