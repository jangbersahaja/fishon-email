import * as React from "react";
interface EmailLayoutProps {
    preview: string;
    children: React.ReactNode;
}
export declare function EmailLayout({ preview, children }: EmailLayoutProps): React.JSX.Element;
export {};
